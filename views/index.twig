{% extends "base.twig" %}

{% block header_extra %}

  <div class="row" style="min-height: 80vh; padding-top:20em;">

    <div class="col-md-6">
      <div style="background: rgba(0, 0, 0, 0.2);">
        <h1 class="hdr-logo" role="banner">
		    {{site.name}}
	    </h1>
      <h2>Tekevän ihmisen olohuone - Makers living room</h2>
      <button class="contrast">VR Tour</button>
      <button>Vierailijan info</button>
      <button>Visitor info</button>
      </div>
    </div>

  </div>

{% endblock %}

{% block content %}
  <div class="">
    <div class="row container">
	    {% for post in posts %}
        <div class="col-md-6">
		      {% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
        </div>
	    {% endfor %}
    </div class="row">
	{% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}
  </div>
{% endblock %}
